import 'package:flutter/material.dart';


class FilterIconWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return IconButton(
      icon: Icon(Icons.filter_list),
      onPressed: () {
        // TODO: Implement filter logic or navigation
      },
    );
  }
}
